% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_percent.R
\name{format_percent}
\alias{format_percent}
\title{Format percentages for publication}
\usage{
format_percent(x, digits = 1, input = "proportion", symbol = TRUE)
}
\arguments{
\item{x}{Numeric vector containing proportions or percentages.}

\item{digits}{A single non-negative integer specifying the number
of decimal places to display. Default is 1.}

\item{input}{Character string specifying how \code{x} should be interpreted.
Must be either \code{"proportion"} or \code{"percent"}.
Default is \code{"proportion"}.}

\item{symbol}{Logical. If TRUE, the percent symbol is appended to
each value. Default is TRUE.}
}
\value{
A character vector containing formatted percentages.
}
\description{
Formats proportions or percentages using publication-friendly
conventions. By default, input values are interpreted as proportions
and converted to percentages.
}
\details{
When \code{input = "proportion"}, values must fall between 0 and 1
and are multiplied by 100 before formatting.

When \code{input = "percent"}, values are assumed to already be expressed
as percentages and are not rescaled.

Missing values are returned as \code{NA}.
}
\examples{
format_percent(0.42)
format_percent(0.423, digits = 2)
format_percent(42, input = "percent")
format_percent(c(0.25, 0.50, 0.75))

}
